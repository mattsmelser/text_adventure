#enemies list
import random

class quill_rat():
    # mob = quill_rat() <- not here its still inside the class
    # mob.print_details() <- same as note above
    health = 40
    attack = random.randint(0,16)
    name = "Quill Rat"
    attack1 = "bite"
    #def print_details(quill_rat):
       # print("You encounter a {}.  It has {} health and it attacks you with {} doing {} damage.".format(name, health, attack1, attack)

mob = quill_rat() # should go outside the class to instantiate
mob.print_details()

